interface Props {
  code: string;
  mode: string[];
  language: string;
}

function generatePrompt(body: Props): string {
  //   switch through the modes an add to request
  let prompt = "Still thinking about how the prompt will be generated...";

  return prompt;
}
